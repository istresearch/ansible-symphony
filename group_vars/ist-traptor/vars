---
# file: group_vars/traptor-nodes/vars

traptor_sentry_secret: "{{ vault_traptor_sentry_secret }}"

traptor_kafka_topic: "{{ vault_traptor_kafka_topic }}"

traptor_track_count: 3
traptor_follow_count: 2
traptor_location_count: 6

traptor_track_apikeys: "{{ vault_traptor_track_apikeys }}"
traptor_follow_apikeys: "{{ vault_traptor_follow_apikeys }}"
traptor_location_apikeys: "{{ vault_traptor_location_apikeys }}"

# Only for production memex cluster.  For vagrant testing, host_vars is used
traptor_es_port: 59200
traptor_log_level: "INFO"
traptor_redis_port: 6379
traptor_redis_db: 1

traptor_type: 'track'

traptor_ist_es: "{{ vault_traptor_ist_es }}"
