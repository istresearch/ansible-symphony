input {
  file {
    path => ['/var/log/grawler/*.log']
    codec => json
    tags => ['grawler']
  }
}

output {
    if 'grawler' in [tags]{
        elasticsearch {
            hosts => "https://ist-es.istresearch.com:9200"
            user => "ist-es"
            password => "44vtjgDLn6Xdj4d8"
            template => "/etc/logstash/templates/logs-template.json"
            template_name => "logs-*"
            template_overwrite => true
            index => "logs-grawler-%{+YYYY.MM.dd}"
            document_type => "%{[logger]}"
        }
    }
}
