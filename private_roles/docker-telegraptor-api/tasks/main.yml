---

- name: Check if Docker is running
  command: service docker status
  ignore_errors: yes
  changed_when: false
  register: service_docker_status

- name: Report status of Docker
  fail:
    msg: |
      Service Docker is not running.
      Output of `service docker status`:
      {{ service_docker_status.stdout }}
      {{ service_docker_status.stderr }}
  when: service_docker_status | failed

- name: Make sure Compose Directory exists
  file:
    path: "{{ docker_compose_dir }}"
    state: directory
    mode: 0755
  tags: docker-telegraptor-api

- name: Make sure scripts directory exists
  file:
    path: "{{ docker_scripts_dir }}"
    state: directory
    mode: 0755
  tags: docker-telegraptor-api

- name: Make sure docker_configs directory exists
  file:
    path: "{{ docker_configs_dir }}"
    state: directory
    mode: 0755
  tags: docker-telegraptor-api

- name: copy compose file
  template:
    src: telegraptor-api-compose.prod.yaml.j2
    dest: "{{ docker_compose_dir }}/telegraptor-api-compose.prod.yaml"
    mode: 0644
  tags: docker-telegraptor-api

- name: copy scripts file
  template:
    src: dc-telegraptor-api.sh.j2
    dest: "{{ docker_scripts_dir }}/dc-telegraptor-api.sh"
    mode: 0644
  tags: docker-telegraptor-api

- name: Stop and Kill any existing Telegraptor API Containers
  command: "{{ docker_scripts_dir }}/dc-telegraptor-api.sh down"
  tags: docker-telegraptor-api

- name: Start the Telegraptor API Container
  command: "{{ docker_scripts_dir }}/dc-telegraptor-api.sh up -d"
  tags: docker-telegraptor-api
